[package]
name = "hintfile"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel = { workspace = true }

configure_me = "0.4.0"

[build-dependencies]
configure_me_codegen = "0.4.0"

[package.metadata.configure_me]
spec = "config_spec.toml"

[[bin]]
name = "construct"
